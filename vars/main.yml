consul_req_packages: 
  - unzip
  - curl
  - ntp
  - supervisor
consul_package: vault_0.1.2_linux_amd64.zip
consul_url: https://dl.bintray.com/mitchellh/vault/"{{ vault_package }}"
consul_loglevel: INFO
consul_qorum: 3
consul_server: false
region: vagrant
